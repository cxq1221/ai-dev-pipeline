# AI 交付平台 HTML 原型

直接双击 `ai-dev-pipeline-prototype.html` 即可运行，无外部依赖。

也可在此目录运行 `python3 -m http.server 8765 --bind 127.0.0.1`，然后打开 http://127.0.0.1:8765/ai-dev-pipeline-prototype.html 。

当前采用版本工作台：每个平台独立管理版本。主需求放入统一需求池，拆分为平台任务，再纳入对应平台版本。原三种布局探索已由版本视图替代。

需求池标题可进入需求详情（`#/requirements/REQ-101`），详情复用需求澄清视图与底部 AI 对话。主需求的说明、修订与对话跨入口共享；可跳到关联平台任务，或返回需求池。未排期需求也可对话，执行开发需选择已排期的平台任务。修改共享需求会更新关联的未发布版本范围；已发布任务保留旧说明。对话仍为模拟，刷新后修改不保留。

演示路径：创建主需求并选择平台 → 自动拆分平台任务 → 管理员调整任务版本 → 独立开发并合入版本 → 构建版本候选包 → 运行回归及人工验收 → 三角色确认 → 模拟内部发布。

每个版本有“概览、需求与缺陷、开发、构建与体验、测试、发布记录”。每个候选包独立绑定范围修订、制品标识和测试结果，并保存构建时任务快照。测试期调整范围会使旧包不再适用于当前范围，需重新构建及验证。已发布版本禁止调入、调出任务，发布快照保留。版本档案可导出 JSON。

全部业务数据为模拟，刷新恢复初始状态。口述按钮填充示例；附件只读取文件名；下载为 JSON 交付清单与档案，不生成真实安装包。没有接入 AI、Git、CI、MeterSphere 或真实发布服务。

测试入口现为执行记录列表，可按编号/候选包搜索及按状态筛选。每次手动执行生成独立报告，使用 `#/test-runs/<执行编号>` 地址；预置报告支持刷新及直接打开，新建模拟报告刷新后不保留。报告含概览与证据、用例与步骤、环境与制品、时间线、缺陷追溯、历史与同步。重新执行不覆盖原报告，自动重试历史保留在用例下。

测试概览展示规则驱动的“AI 测试结果研判”模拟：总体结论、需求与验收目标达成情况、风险与原因分析、发布建议、行动及完成标准。根据本次执行状态和用例结果区分完成、失败、重试、跳过及未执行；不调用真实 AI，不自动批准发布。

截图示意、录屏采集状态和模拟日志保留在用例详情中，概览不再展示证据卡片。点击证据打开右侧面板。截图明确标记为设计示意；当前没有真实视频文件，因此显示“未采集”，不提供虚假的播放能力。支持逐条模拟执行、取消、人工验收说明及导出执行 JSON。首批完整示例围绕弱网需求，其他需求不套用该示例用例。

版本详情新增“需求澄清”：按平台任务展示原始输入、详细行为与范围、验收标准、待确认问题、补充说明、修订历史及用户确认。弱网示例附可交互线框原型和组件关系图。其他需求展示待完善骨架，不伪装为 AI 已完成分析。修改澄清会更新范围修订，使旧候选包不能用于当前范围发布。

开发页提供失败、修复、审查和合入的七步历史回放，以及示例差异与证据缺口；测试页提供候选包切换、逐条模拟执行、用例状态筛选、预期/实际结果与日志、重试历史、人工验收说明和 MeterSphere 模拟对账。没有采集的截图和录屏明确标为缺失。

浏览器验证包含需求澄清确认、80/81ms 交互边界、旧包禁用、失败用例详情；原有流程已验证需求创建、调版、构建、审批与模拟发布。布局探索属于历史版本，当前入口统一为版本工作台。
