# 分支与 Tag 策略（原型）

入口：产品仓库 → 业务 Web（共享仓库示例）→ 分支与 Tag 策略。

## 已确认规则

- 策略按代码服务、业务线、仓库映射维护。
- 原力稳定分支 `yl_main`，测试集成分支 `yl_test`。
- 开发模板 `yl_dev_{feature}_{seq}`：任务 205 首次为 `yl_dev_feature205_1`，重开分支递增序号；普通提交不递增。
- 测试模板 `yl_test_{version}.{batch}`，发布模板 `yl_release_{version}.{batch}`。
- `version` 是三段产品版本；`batch` 为提测批次，新提测递增，发布沿用测试通过的批次。
- 所有模板、分支及起始批次可维护；已有 Tag 不覆盖。
- 自建和原力共享示例仓库地址，自建策略待配置，不猜测实际分支名称。
- 版本、开发、触发演示保存策略快照，后续策略修改不覆盖旧快照。

## 演示流程

维护策略时实时预览任务号、版本和序号对应的名称。命名与触发演示支持新建模拟开发和提测、模拟失败或全量通过、合入稳定分支并模拟校验内容，以及显式触发发布；失败记录保留。

## 边界

没有创建真实 Git 分支、Tag、MR 或执行 CI。命名触发演示使用独立内存记录，尚未与原有版本测试报告、制品发布记录合并；构建、部署、测试、内容一致性校验均为明确标注的模拟步骤。真实集成还需 GitLab 事件校验、权限、受保护分支/Tag、幂等、提交可达性及制品一致性验证。
