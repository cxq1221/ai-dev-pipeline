# 产品仓库与多仓库协作原型

入口：`ai-dev-pipeline-prototype.html` 左侧「产品仓库」。所有数据保存在当前页面内存中，刷新重置；无真实 AI、GitLab、代码执行或发布。

## 已确认模型

- 产品包含多个代码服务；服务指定主要研发小组及协作小组。
- 产品支持多个业务线：自建、原力、IaaS、咪咕、云电竞。
- 服务 × 业务线可配置多条仓库、默认开发分支、仓库目录映射。
- 版本选择产品、业务线、同平台服务，保存当时的映射快照。配置变更不覆盖历史版本或开发快照。
- 主需求可以拆分多个仓库任务，各自绑定交付版本；共享业务说明，不复制主需求。
- 仓库任务支持前置依赖和发布顺序说明，循环依赖不能保存。
- 联合验收绑定确切模拟提交和制品，每次新建记录；单仓验证不等于联合验收，联合通过也不自动发布。

## 操作演示

1. 产品仓库 → 新增产品 → 添加代码服务 → 添加仓库映射。
2. 版本工作台 → 创建版本 → 选择产品、业务线、服务映射，填写同平台版本号。
3. 产品仓库 → 打开多仓库需求示例，演示 Android SDK + cg-session。
4. 分别「开始开发」「模拟完成并单仓验证」，再发起联合验收，填写用户视角范围。
5. 模拟失败；重新开发相关任务，产生新提交和制品，再创建联合验收。旧记录不被覆盖。
6. 修改基础映射后查看已有版本：旧配置快照仍然保留；如需采用新配置，在版本概览中显式更新。

## 当前边界

示例归属关系不代表公司真实配置。AI 服务建议只是引导手动确认，不做真实推断。发布先后顺序为记录字段，尚无自动部署编排。原有单仓测试报告仍使用弱网示例，跨仓库测试使用独立的联合验收模拟记录。尚无持久化、权限控制、GitLab 同步、真实提交或制品。
