# 测试资源原型

入口：侧栏「测试资源」，包含「测试环境」和「测试设备」。所有配置、连接状态与占用均为内存模拟，刷新恢复；没有真实设备连接、远程控制或外部写入。

## 管理范围

- 环境：名称、biz 业务标识、可选 GID、AK 标识、SK 凭据引用、环境类型、业务线、网络区域、负责人、支持平台、服务地址、可选访问账号、依赖服务与资源池、启用状态。访问账号仅记录用户名；SK 只记录 Vault／K8s Secret 引用，不保存或展示明文。
- 设备：名称、平台、实体机／模拟器／浏览器节点、系统与浏览器版本、可选 IP、访问地址、访问账号、启用状态、模拟在线状态、最后心跳。访问账号仅记录用户名，不保存密码。
- 支持新增、编辑、筛选、启用／停用。执行中的设备及使用中的环境禁止编辑、停用；设备关联当前运行记录。
- 环境不固定绑定设备，执行时组合选择。没有多设备并行编排、队列、抢占、远程重启或画面控制。

## 测试流程

版本测试页、制品页发起测试、报告重新执行统一打开选择框：一个候选包、一个业务线与平台匹配的环境、一台兼容且启用、在线、未占用的设备。提交时再次校验可用性。

每次执行深拷贝环境、设备与采集时间到 `resourceSnapshot`。报告「环境与制品」、证据抽屉及 JSON 导出读取快照，后续配置调整不修改历史。原有报告没有快照时显示「未记录」。

占用由运行状态推导，完成和取消立即释放。执行仍使用原有弱网示例用例，并由「模拟执行下一条」推进；没有配置示例用例的版本不伪造测试结果。

## 已验证

- 测试环境新增必填校验、成功保存；设备编辑保存。
- 三个测试入口均经过资源选择。
- 执行后设备显示占用、关联运行记录，并禁止重复选择。
- 取消及完成六条模拟用例后设备释放。
- 更新设备名称后，旧报告仍保留原名称与系统信息。
- 停用设备在选择框中不可选；平台、业务线在资源列表中约束。

剩余生产接入：持久化、身份权限、执行器与设备心跳、并发事务锁、真实运行取消及异常退出释放、凭据管理、真实证据采集。
