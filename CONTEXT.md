# AI 交付编排

本上下文描述从统一对话进入需求、调度执行 Agent、控制 SDK 运行时并沉淀交付证据的核心语言。

## Language

**统一对话**:
围绕同一需求和交付任务持续发生的用户与执行 Agent 交流，覆盖需求澄清、执行追问、进度、风险和结果，不区分上层或下层对话流。
_Avoid_: 上层 AI 对话、下层 AI 对话

**Agent 统一调度网关**:
把统一对话与需求、执行上下文、执行状态和证据关联起来，并根据目标平台、仓库、能力和可用性选择执行 Agent 与机器、管理任务租约的中心概念；它本身不承担 LLM 推理。
_Avoid_: LLM 网关、模型网关、独立智能调度器

**执行 Agent**:
在被调度机器上调用 LLM 和本机工具完成计划、开发、测试、控制与验证，并通过统一对话进行实际交流的自主执行者。
_Avoid_: 上层 Agent、下层 Agent

**执行快照**:
一次执行开始时固定的需求修订、平台任务、仓库范围、约束和验收目标；后续对话变化不会隐式改变正在运行的执行。
_Avoid_: 实时需求、浮动上下文

**主执行 Agent**:
一次执行中唯一拥有工作区写权限的执行 Agent；其他并行执行 Agent 只能承担测试、审查或诊断等只读工作。
_Avoid_: 多主 Agent、并行写 Agent

**SDK Runner**:
随目标 App 或 SDK 运行时部署、只执行受限运行时指令并回传状态与证据的执行者，不是独立 AI Agent。
_Avoid_: SDK Agent
